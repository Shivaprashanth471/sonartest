name: Deploy DEV-API to AKS Cluster
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        fetch-depth: 2
    - name: Clone supercharge docs repository
      uses: actions/checkout@v2
      with:
        repository: shivaprasanth471/tericsoft-services/vitawerks_automation    
    
    # - name: Git Clone Action
    #   uses: sudosubin/git-clone-action@v1.0.1

    #   with:
    #     repository: 'shivaprasanth471/tericsoft-services/vitawerks_automation'
        # platform: 'github'
    #   run: |
    #     git clone https://github.com/shivaprashanth471/tericsoft-services/vitawerks_automation.git
    #     cd vitawerks_automation
    #     install maven
    #     mvn test -Dtest=Pipeline1_Api_Smoke mvn test -Dtest=Pipeline1_Web_Smoke
     



















































# # name: Vitawerks Test CI&CD setup

# # on:
# #   push:
# #     branches:
# #       - "main"

# # jobs:
# #   build:
# #     runs-on: ubuntu-latest
# #     steps:
# #       - uses: actions/checkout@v1

# #       - uses: actions/checkout@master

     
# #       - name: Install Node.js
# #         uses: actions/setup-node@v1
# #         with:
# #           node-version: "14.x"
# #       # - name: Scan with Sonar Scanner
# #       #   uses: mathrix-education/sonar-scanner@1.0.0

# #       # # - uses: mathrix-education/sonar-scanner@master
# #       #   with:
# #       #     version: 4.2.0.1873 # required
# #       #     typescript: true
# #       #     scan: true
# #       #     args: --debug -Dsonar.login=${{ secrets.SONARQUBE_TOKEN }}

# #     # - name: SonarCloud Scan
# #     #   uses: sonarsource/sonarcloud-github-action@master
# #     #   env:
# #     #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
# #     #     SONAR_TOKEN: ${{ secrets.SONARQUBE_TOKEN }}      
# #     - name: SonarQube Scan
# #       uses: sonarsource/sonarqube-scan-action@master
# #       env:
# #         SONAR_TOKEN: ${{ secrets.SONARQUBE_TOKEN }}
# #         SONAR_HOST_URL: ${{ secrets.SONARQUBE_HOST }}
# #       # - name: SonarQube Scan
# #       #   uses: sonarsource/sonarqube-scan-action@master
# #       #   env:
# #       #     SONAR_TOKEN: ${{ secrets.SONARQUBE_TOKEN }}
# #       #     SONAR_HOST_URL: ${{ secrets.SONARQUBE_HOST }}
# #       #     LC_ALL: "ru_RU.UTF-8"

# #       # - name: Install npm dependencies
# #       #   run: make deploy-application STAGE=test
# #       # - name: SonarQube Scan
# #       #   uses: kitabisa/sonarqube-action@v1.1.0
# #       #   with:
# #       #     host: ${{ secrets.SONARQUBE_HOST }}
# #       #     login: ${{ secrets.SONARQUBE_TOKEN }}
     
# #   #    sonar-scanner \
# #   # -Dsonar.projectKey=ssssss \
# #   # -Dsonar.sources=. \
# #   # -Dsonar.host.url=http://3.6.47.66:9000 \
# #   # -Dsonar.login=142291a98f198441fb84a2a3ea268dac1ca5bd87